rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read and write quiz results (no authentication required)
    match /quizResults/{document=**} {
      allow read, write: if true;
    }
  }
}
